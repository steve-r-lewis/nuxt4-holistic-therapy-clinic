# @monorepo/theme-manager

The @monorepo/theme-manager module provides a centralized approach to managing and configuring frontend theme styling. It allows developers to easily switch between different themes, each with its own unique set of styles. This module is designed to be highly customizable and extensible, making it easy to integrate into existing projects.
